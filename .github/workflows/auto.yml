name: Google IDX Auto Visit (24h保活)

on:
  schedule:
    - cron: "*/15 * * * *"  # 每15分钟执行一次
  workflow_dispatch:

jobs:
  keepalive:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Run Visit Task Checker
        run: |
          echo "🔍 检查访问任务状态..."
          node auto_visit.cjs & sleep 10
          ps aux | grep auto_visit.cjs

      - name: Commit Latest Status
        if: always()
        run: |
          git config --global user.email "github-actions@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          git add status.json || true
          git commit -m "update status $(date '+%Y-%m-%d %H:%M:%S')" || echo "no changes"
          git push
